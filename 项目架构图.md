### AutoLLM-TestFramework 项目架构

```text
                    +---------------------------+
                    |       CLI 入口: run.py     |
                    +-------------+-------------+
                                  |
                          { 模式选择 (Mode) }
            ____________/         |         \____________
           /                      |                      \
    [ generate ]               [ all ]                  [ run ]
         |                        |                        |
         V                        V                        V
+-------------------+      +--------------+      +-----------------------+
|   CaseGenerator   | <----+ 一键全流程模式 +----> | pytest + test_executor |
+---------+---------+      +--------------+      +-----------+-----------+
          |                                                  |
    +-----+-----+           +----------------+         +-----+-----+
    | DocParser | --------> |  OpenAIClient  | <-------+  AIJudge  |
    +-----+-----+           +-------+--------+         +-----+-----+
          |                         |                        |
    +-----+-----+           +-------+--------+         +-----+-----+
    | Prompt模板|           | settings.yaml  |         |  requests |
    +-----------+           +----------------+         +-----+-----+
          |                                                  |
    +-----+-----+           +----------------+         +-----+-----+
    | 生成用例  | --------> |data/test_cases | <-------+ 参数化加载|
    +-----------+           +----------------+         +-----------+
                                                             |
                                                       +-----+-----+
                                                       |allure-res |
                                                       +-----+-----+
                                                             |
                                                       +-----+-----+
                                                       |allure-repo|
                                                       +-----------+

 [ 辅助工具 ]
 +-----------------------+      +-----------------------+
 | utils/logger (日志)    | <--> | utils/file (文件处理)  |
 +-----------------------+      +-----------------------+

```

---

### 💡 核心链路解读

1. **生成链路**：从 `raw_docs` 开始，经过 `DocParser` 解析，配合 `prompt_templates.yaml` 的提示词，由 `OpenAIClient` 调用大模型生成 JSON 格式的用例并存入 `data/test_cases`。
2. **执行链路**：`pytest` 自动加载 `data/test_cases` 下的 JSON 文件进行参数化测试，通过 `requests` 库发送请求。
3. **判定链路**：`AIJudge` 会根据你的配置（`exact_match` 或 `semantic_match`）来决定是直接比对字符串还是把结果扔回给 LLM 做语义判定。
4. **报告链路**：测试数据实时写入 `allure-results`，最后由你刚才安装的 **Allure CLI** 渲染成漂亮的 `allure-report`。
